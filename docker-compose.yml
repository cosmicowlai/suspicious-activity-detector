version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      PYTHONUNBUFFERED: "1"
    command: uvicorn suspicious_activity_detector.api:app --host 0.0.0.0 --port 8000
